# Base URL for the chat backend. Leave as /api to use the Vite dev proxy
# (see vite.config.ts) or the production reverse proxy.
VITE_API_BASE_URL=/api

# Only used by the Vite dev server proxy target.
VITE_PROXY_TARGET=http://localhost:8000
